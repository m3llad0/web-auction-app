export interface Product {
    id: string;
    name: string;
    currentBid: number;
    description: string;
    starting_date: string;
    finish_date: string;
    created_by: string;
    img: string;
}